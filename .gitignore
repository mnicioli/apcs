# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# testing
/coverage

# supabase (artefatos locais — NUNCA commitar)
/supabase/.branches
/supabase/.temp
/supabase/.env

# env files (segredos — NUNCA commitar)
.env
.env.*
!.env.example

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# os / editor
.DS_Store
*.pem
.idea/

# vercel
.vercel

# skills instaladas por conta própria (ferramenta pessoal, não do time).
# As skills do projeto — new-feature, new-migration, pre-pr, review... — são
# versionadas e continuam entrando normalmente; a exclusão é só desta, cuja
# orientação (tipografia e paleta próprias por tela) contraria o design system
# fechado descrito no CLAUDE.md.
/.claude/skills/frontend-design/

# typescript build info
*.tsbuildinfo

# Material de referência recebido do cliente (contém .env). Nunca vai para o Git.
/ToDo/
